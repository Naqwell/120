#include<iostream>
использование пространства имен std;
float max(float x, float y); // функция объявления
интервал основной()
  {
    float a = 5,5, b = 3,2, c = 14,1, d;
    d = max(max(a,b),c); // 1
    cout << "max = " << d << endl;
    вернуть 0;
  }

  float max(float x, float y) // определение функции
  {
     вернуть (х > у)? х: у;
  }
